# Color Today

Static webpage used to generate a "Color of the Day". The color is mostly unique, because it uses a SHA256 hash of the date to calculate a color hex code. It also displays 4 additional colors to create a color palette for the day.
